
let cryptol_mod = cryptol_load "SABER.cry";
let llvm_mod = llvm_load_module "SABER.o";

let matvecmul_spec = {{
  pre : {{ A, s, transpose }} -> true,
  post : {{ A, s, transpose, result }} -> result == MatrixVectorMul_Cryptol(A, s, transpose)
}};

llvm_verify llvm_mod "MatrixVectorMul" matvecmul_spec;
