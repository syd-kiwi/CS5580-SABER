
let cryptol_mod = cryptol_load "SABER.cry";
let llvm_mod = llvm_load_module "SABER.o";

let bs2polmsg_spec = {{
  pre : {{ bytes }} -> true,
  post : {{ bytes, msg }} -> msg == BS2POLmsg_Cryptol(bytes)
}};

llvm_verify llvm_mod "BS2POLmsg" bs2polmsg_spec;
