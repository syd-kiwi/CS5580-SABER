
let cryptol_mod = cryptol_load "SABER.cry";
let llvm_mod = llvm_load_module "SABER.o";

let polyvecq2bs_spec = {{
  pre : {{ s }} -> true,
  post : {{ s, bytes }} -> bytes == POLVECq2BS_Cryptol(s)
}};

llvm_verify llvm_mod "POLVECq2BS" polyvecq2bs_spec;
