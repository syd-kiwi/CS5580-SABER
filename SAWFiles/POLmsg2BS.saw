
let cryptol_mod = cryptol_load "SABER.cry";
let llvm_mod = llvm_load_module "SABER.o";

let polmsg2bs_spec = {{
  pre : {{ msg }} -> true,
  post : {{ msg, bytes }} -> bytes == POLmsg2BS_Cryptol(msg)
}};

llvm_verify llvm_mod "POLmsg2BS" polmsg2bs_spec;
