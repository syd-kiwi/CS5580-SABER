
let cryptol_mod = cryptol_load "SABER.cry";
let llvm_mod = llvm_load_module "SABER.o";

let gensecret_spec = {{
  pre : {{ seed }} -> true,
  post : {{ seed, s }} -> s == GenSecret_Cryptol(seed)
}};

llvm_verify llvm_mod "GenSecret" gensecret_spec;
